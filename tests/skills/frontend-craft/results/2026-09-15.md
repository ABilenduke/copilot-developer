# Frontend Craft evaluation — 2026-09-15

Run logs, generated applications, screenshots, and detailed evidence are local-only,
Git-ignored artifacts. References marked `local artifact` are unavailable in a fresh
checkout; this dated findings summary is retained in version control.

**Status:** Complete — 24 comparative trials, nine activation probes, and separate
visual, technical, and workflow reviews are recorded. The candidate is not promoted.

## Method and provenance

The clean pilot is `/tmp/frontend-craft-pilot-v2`. It uses Codex CLI 0.154.0,
gpt-6-astra with ultra reasoning, four concurrent workers, and a 720-second agent
execution limit for each of sixteen initial trials and eight finalist repeats.
The four initial conditions are native
Codex, pinned Anthropic plus locally pinned Vercel guidance, pinned Impeccable,
and the frozen Frontend Craft candidate. Each receives the same brief, starter,
browser access, supplied assets, and settings for a given case.

Sources and source-tree hashes are retained in the pilot manifest. The baseline
downloads came from the exact revisions in the candidate's
[provenance record](../../../../skills/frontend-craft/references/sources.md).
The Impeccable baseline includes its Codex payload and checksum-verified 0.1.5
engine. Hooks, plugins, image generation, memories, and delegation are disabled
equally: this evaluates skill workflows, not the complete Impeccable plugin.

Python Playwright operates cached Chromium. Independent functional probes and
axe-core 4.10.3 supplement blind desktop/mobile review. The axe bundle SHA-256 is
`880970c081707360e64f34cea25ff91892f5bc95675b0776925b9709dd8a68bb`.
Automated results report violations and incomplete checks; they do not establish
whole-page WCAG conformance. Parallel wall times are observations, not controlled
performance measurements.

Full prompt inspection also found shared host-injected Cylinder/Vue/BEM/WCAG 2.1
instructions and a proactive delegation mandate, despite disabling local project
documents and multi-agent tooling. The actual skill catalogs are isolated; the
native condition is not a blank prompt. All conditions share this context and the
explicit fixture brief. Failed delegation attempts occur in the traces; successful
independent review cannot be inferred from those attempts.

## Packaging and harness checks

- Skill creator validation, metadata parsing, local reference resolution, scoped
  formatting, and fixture JavaScript lint pass.
- An independent static review found no actionable skill defects. Selected
  accessibility/SEO claims were checked against primary standards. This is not
  evidence of improved generated designs.
- Eleven runner integrity tests pass. They cover skill catalog parsing, hidden
  global-skill exclusion, frozen fixture parity, private rubric separation, unique
  ports, occupied actor-port rejection, interrupted/concurrent execution protection,
  observed usage, and a functional activation fixture that demonstrably fails
  uppercase queries before repair.
- Independent review verified the fixes for render-server ownership, unfinished
  trial rejection, and preservation of interrupted execution evidence.
- Browser probes were exercised against the original fixtures. They identified
  the planted mobile overflow, form-label, validation/duplicate-submission, and
  article structure/indexing problems. Axe detected label/select-name violations;
  a separate validation-state check confirmed low-contrast error text.
- Fresh Codex catalogs show Cylinder only inside the active Cylinder repository;
  see scope evidence (local artifact: `cylinder-scope.json`). Original skill files are preserved.
- Repository-wide ESLint has a pre-existing Prettier error in
  `scripts/readme-update.js:639`; that file was not changed.

## Invalid initial attempt

`/tmp/frontend-craft-pilot` is excluded from comparison. Its first batch was stopped
after discovering that `debug prompt-input` can hide a globally disabled skill
that `exec --ignore-user-config` can re-enable. The runner now explicitly disables
all host skill entrypoints, including ones omitted from the visible catalog. The
clean v2 preflight verifies fourteen excluded host paths and the expected local
catalog for each condition. Partial first-attempt outputs remain diagnostic only.

## Comparative findings and adoption

All sixteen initial implementations and their reviews are recorded. **Frontend
Craft has not earned global promotion.** It produced useful landing, booking, and
article results, but its Vue output remained unfinished and failed mobile use at
the time limit. The two upstream configurations were selected for fresh repeats.

| Configuration | Landing | Vue operations | Booking | Article | Completed within 720 seconds |
| --- | ---: | ---: | ---: | ---: | ---: |
| Native Codex | 4.25 | 4.25 | 4.00 | 1.50 | 0/4 |
| Anthropic + Vercel | 4.00 | 4.50 | 4.00 | 4.75 | 1/4 |
| Impeccable | 4.75 | 4.50 | 4.00 | 4.50 | 1/4 |
| Frontend Craft | 4.50 | 1.75 | 4.00 | 4.50 | 0/4 |

Scores are means of each brief's ordinal 1–5 design criteria, assigned by a
separate model reviewer who saw only briefs, criteria, and screenshots. Small
differences are not decisive: all booking outputs tie, and several public-page
differences are modest. These are not human preference ratings or statistical
evidence. Technical review was independent and separate from visual scoring,
but was not strictly blinded to configuration identity.

The independent browser review of the initial stage found:

- **Frontend Craft's Vue result (003):** 1192px document width at a 390px viewport,
  with desktop and mobile presentations both visible. New markup retained the
  starter stylesheet. Core data interactions and the build pass; the delivered
  layout fails the brief.
- **Native's article (011):** 940px mobile overflow, missing script/brand resources,
  and an unfinished stylesheet. Native's form placeholder (009) and Vue count
  (012) also have measured contrast below the applicable 4.5:1 threshold.
- **Upstream results:** the required observed flows work across all four briefs.
  Minor defects remain, including generic-wrapper ARIA naming, transient focus
  continuity, and Impeccable's last-section navigation indicator (015). Its own
  resize assertion did not reproduce as a persistent failure after settling.

Fourteen runs reached the limit. Only Impeccable's Vue run (631.8 seconds) and
Anthropic/Vercel's form run (644.7 seconds) completed within it. A success-style
final from Impeccable's form (002) arrived at the timeout boundary; its recorded
status remains timed out. These are outputs under a bounded budget, not proof of
the configurations' maximum quality or a general speed ranking. The cap was
enforced by the harness rather than presented as an explicit prompt deadline.

Only three initial traces contain usage records; missing usage is unknown, not
zero. No total-token or monetary-cost ranking is justified. The captured CLI
stream records screenshot creation but not image-view events, so it cannot
independently prove that each implementing agent inspected its screenshots.

Inspect the screenshot comparison (local artifact: `pilot-2026-09-15/index.html`),
blind ratings (local artifact: `pilot-2026-09-15/reviews/visual/initial-ratings.md`),
technical evidence (local artifact: `pilot-2026-09-15/reviews/technical/technical-findings.json`),
and workflow review across all 24 trials (local artifact: `pilot-2026-09-15/reviews/workflow/final-summary.md`).
The screenshot comparison and detailed review archive are Git-ignored and are not
shipped in a fresh checkout. This findings summary remains versioned.

Trial 010 (Anthropic plus Vercel, Vue) encountered an unrelated service on its
assigned port. Its agent worked around the collision by rendering the production
build through browser request interception. Independent post-run browser checks
can assess the resulting interface, but its execution time and verification
progress are environmentally confounded. Future trial creation now chooses and
records an available unique actor port, and preflight/execution reject collisions
before model work. Original prompts and results remain unchanged. A race between
the availability check and a later server start cannot be completely excluded.

Eight fresh repetitions of Anthropic/Vercel and Impeccable used the frozen inputs
and original settings. The selection rationale (local artifact: `pilot-2026-09-15/finalist-rationale.md`)
was written after all initial visual ratings were fixed. The candidate and main
comparison starters remain unchanged between stages. Activation setup corrections
are recorded separately below and do not replace the original probe records.

| Repeated configuration | Landing | Vue operations | Booking | Article | Completed within 720 seconds |
| --- | ---: | ---: | ---: | ---: | ---: |
| Anthropic + Vercel | 4.50 | 4.50 | 4.00 | 4.25 | 1/4 |
| Impeccable | 4.00 | 1.00 | 4.00 | 1.50 | 0/4 |

Impeccable's repeat article (020) lacks its referenced stylesheet and overflows
to 948px at the narrow viewport. Its repeat Vue app (023) imports a missing
stylesheet and fails to build; the screenshots show an error overlay. That run's
1.00 rating describes the delivered visual outcome, not an assessment of an
unseen intended design. The form ratings remain tied. These differences support
preserving the shortlist decision and recording the repeat failures, not treating
one attractive screenshot as sufficient evidence for adoption.

Independent checks confirm the repeat booking flows, including trial 021's error
recovery: a raw probe rejected different error-message wording, while direct
interaction demonstrated retained inputs and successful retry. Both observations
are preserved. Trial 024's article meets the core reading, navigation, responsive,
and indexing requirements, with a minor lag in its current-section indicator.

Only Anthropic/Vercel's repeat landing run (019, 686.9 seconds) completed within
the limit. Across both stages, **3 of 24 runs completed and 21 timed out**. This
heavy censoring makes the pilot unsuitable for a broad quality or speed ranking.
Four comparison traces contain usage records; totals for the other twenty remain
unknown. The repeat evidence also contains material upstream failures, so the
initial shortlist is not a reliability guarantee.

The repeat visual review (local artifact: `pilot-2026-09-15/reviews/visual/repeat-ratings.md`)
and comparison summary (local artifact: `pilot-2026-09-15/comparison-summary.json`) retain the
criterion scores alongside process and technical evidence. The summary also
confirms that every frozen source and starter is unchanged and that the current
candidate is byte-for-byte the tested candidate.

### Adoption decision

**Keep Frontend Craft as an experimental repository skill; do not install it
globally.** Its initial operational-interface failure prevented selection for
repetition, so there is no repeated evidence of the required advantage across
expressive and product-interface work. This is the adoption rule operating as
intended, not a claim that custom skills cannot help.

Retain the existing global design authority and browser tooling. Among the tested
setups, the lean Anthropic/Vercel combination is the provisional choice for further
use and comparison. The heavily censored pilot does not justify a blanket
superiority claim or silently replacing existing global installations. The exact
tested candidate remains unchanged and available for opt-in experiments.

The clearest follow-up target is reliable completion of integrated markup and CSS
before spending remaining effort on refinement. Any revision should address that
observed failure, then enter a new frozen comparison. Adding more aesthetic rules
is not supported by this evidence. A future trial should also communicate its
deadline, use a practical completion budget, eliminate the inherited Cylinder
context where possible, and obtain human preference review before a strong design
quality claim.

Cylinder is now discoverable only in its owning project. The original global
files and configuration backup are preserved, and the candidate was not installed
in either global skill directory.

## Activation evidence

The eight initial probes show the intended selection: all four design requests
successfully read Frontend Craft; all four narrow non-design requests completed
without reading it or changing styling/design. Positive probes reached their
180-second limit, so this is activation evidence rather than completed-task
evidence. The backend probe changed SQL/documentation, the typo probe changed one
word, and the import probe removed the broken import.

Two setup limitations remain explicit: the new-page request asked for ceramics
while supplying printmaking assets, and the functional Vue request found an
already-correct case-insensitive predicate. The latter verified existing behavior
without making changes. A ninth, supplementary probe used a fresh copy with the
query normalization deliberately removed. It completed in 63.4 seconds, repaired
the one-line predicate, read no design skill, and left markup and styling unchanged.
Its captured build and browser checks pass. Original records remain unchanged;
future probe setup now supplies the actual defect and a consistent printmaking
brief. The visual-critique request authorizes improvements and therefore does not
test review-only restraint.

See the nine-probe activation summary (local artifact: `pilot-2026-09-15/reviews/workflow/activation-summary.md`),
original eight probes (local artifact: `pilot-2026-09-15/reviews/workflow/activation-review.md`), and
supplementary repair evidence (local artifact: `pilot-2026-09-15/reviews/workflow/activation-supplementary-review.md`).
