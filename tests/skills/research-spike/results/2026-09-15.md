# Research Spike results — 2026-09-15

Run logs, generated applications, screenshots, and detailed evidence are local-only,
Git-ignored artifacts. References marked `local artifact` are unavailable in a fresh
checkout; this dated findings summary is retained in version control.

## Observed outcomes

All four candidate tasks met their bounded expectations. The evaluator inspected real repository
files, ran disposable Python probes, and preserved source, user notes, HEAD and staged diff. Only the
two authorized research reports changed.

| Case | Actual result |
| --- | --- |
| Local round trips | Answered for tested values; Unicode/null/nested values preserved through JSON and UTF-8; no repository writes. |
| Production throughput | Unresolved; local encoder timing explicitly did not establish 10,000 production requests/sec. Report saved. |
| Missing number contract | Unresolved; demonstrated potential precision changes without selecting unspecified product semantics; no repository writes. |
| Resume after revision | Existing report updated; moved source inspected, base64 removed from active scope, unsupported historical benchmark labeled unusable. |

The reports include full probe scripts, runtime, commands, inputs, actual output, and limitations.
The throughput report notes concurrent probe activity and treats timings as illustrative; it does not
compare unequal encoders as a valid production-performance baseline. Local JSON recommendations are
limited to tested JSON-compatible values rather than all Python values or live consumer compatibility.

## Evidence

- Requests (local artifact: `candidate-tasks.json`), starting snapshots (local artifact: `candidate-before.json`).
- Actual responses, commands and probe output (local artifact: `candidate-results.json`).
- Independent artifact and preservation inspection (local artifact: `candidate-artifacts.json`), including saved reports.

One fresh worker received the skill and raw tasks without inherited author conversation, grading
expectations, or fixture snapshots. Cases shared its context. A follow-up reminded it to retain
reproducible evidence and stop when the question was settled or bounded, restating skill instructions.
Manual review checked findings, observations, qualified recommendations, and scope revision handling.

## Fresh global invocation

A fresh Codex CLI session used `exec --ephemeral --ignore-user-config --sandbox workspace-write` in
`/tmp/research-spike-global/roundtrip`. It discovered the global skill, inspected the current encoder,
and ran in-memory local probes. Six round trips passed across both Unicode escaping settings; current
`str(value)` outputs failed JSON decoding on the tested samples. The response limited its conclusion
to local behavior and identified the untested consumer boundary.

- Native-client events (local artifact: `global-events.jsonl`).
- Final response (local artifact: `global-final.md`).
- Independent preservation inspection (local artifact: `global-artifacts.json`). Only the roundtrip case was invoked;
  other generated cases were untouched controls.

No repository report, source, user-note, or Git changes occurred during this chat-only invocation.
The collision-checked global symlink points to the repository source. This was explicit invocation;
implicit skill selection was inspected in metadata but not tested through client routing.

## Packaging and limits

Skill validation, YAML metadata checks, local reference links, scoped Prettier formatting, Git inclusion
of references, and `git diff --check` pass. The entrypoint contains 891 whitespace-delimited words.
README documents installation, report saving, Plan Mode, experimental limits, and planning handoff.
The original Claude research workflow is unchanged.

The legacy spike workflow was inspected, not executed as a comparative baseline. Mandatory time-box
questions and automatic GitHub operations were removed; focused investigation and actionable next steps
were retained. No superiority in accuracy, cost, latency, or token use is claimed.

All fixtures were intentionally offline. Live source discovery, citation accuracy, conflicting external
documentation, paid tools, native Plan Mode UI, and explicit time-budget exhaustion were not exercised.
These remain instruction-reviewed behaviors. The probes establish local behavior and honesty about
missing evidence, not production performance or general research reliability. Existing repository-wide
formatting issues are outside this change; new source uses scoped checks.
