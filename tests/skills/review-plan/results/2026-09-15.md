# Review Plan results — 2026-09-15

Run logs, generated applications, screenshots, and detailed evidence are local-only,
Git-ignored artifacts. References marked `local artifact` are unavailable in a fresh
checkout; this dated findings summary is retained in version control.

## Outcome

Both ordinary Codex and the candidate produced the expected substantive conclusions on four real
review fixtures. Every file, including ignored files, and HEAD, staged diff, and Git status remained
unchanged. This sample supports a consistent review contract; it does not demonstrate higher accuracy
than ordinary Codex.

| Scenario | Ordinary Codex | Candidate |
| --- | --- | --- |
| Green tests omit nonstring behavior | Identified P2 wrong exception | Changes needed; identified P2 wrong exception |
| Fully implemented requirements | No findings | No findings |
| Required checker unavailable | Required release verification blocked | Verification incomplete, without inventing a code defect |
| Removed scope and stale journal | Excluded removed export requirement | No findings against revised scope; explained stale journal |

No missed seeded defects, invented implementation defects, unnecessary user questions, or false full
completion claims were observed. Both variants reviewed real code, assertions, direct call outputs,
and current plan scope. Neither accepted the journal's Complete claim as sufficient evidence.

## Evidence

- Ordinary Codex: responses and commands (local artifact: `baseline-results.json`), requests (local artifact: `baseline-tasks.json`),
  starting snapshot (local artifact: `baseline-before.json`), independently inspected artifacts (local artifact: `baseline-artifacts.json`).
- Candidate: responses and commands (local artifact: `candidate-results.json`), requests (local artifact: `candidate-tasks.json`),
  starting snapshot (local artifact: `candidate-before.json`), independently inspected artifacts (local artifact: `candidate-artifacts.json`).

Each variant used a separate fresh worker with no inherited conversation. Workers received the same
requests and equivalent repositories; only the candidate received the skill. They were not given the
driver, oracle, author conclusions, or other results. Cases within a worker shared context. The ordinary
baseline ran while the skill was being authored, rather than as a historically earlier release.

The assessor verifies preservation and captures source and test output. Correctness of review findings
was assessed manually against those artifacts. These small Python cases are qualitative probes, not
statistical evidence or coverage of larger integrations, migrations, permissions, or ambiguous plans.

Candidate command logs include failed attempts to use unavailable `python`, followed by successful
`python3` reads. The failures are disclosed in its reports. The skill already directs repository-derived
checks; no special Python rule was added for this fixture. No check-count, latency, or token-efficiency
improvement is claimed.

## Fresh global invocation

A fresh Codex CLI session used `exec --ephemeral --ignore-user-config --sandbox read-only` in
`/tmp/review-plan-global/gap`, outside the source repository. It found the installed global skill,
read its instructions, ran safe checks, and correctly reported the unmet TypeError requirement despite
the passing suite and Complete journal claim.

- Native-client events (local artifact: `global-events.jsonl`).
- Final review (local artifact: `global-final.md`).
- Independent post-review preservation check and artifacts (local artifact: `global-artifacts.json`). Only the gap
  case was reviewed by this client; the other three global fixtures remained untouched controls.

The symlink at `~/.agents/skills/review-plan` points to the repository's `skills/review-plan` source.
Installation was collision-checked. The session explicitly invoked the skill; implicit routing and the
actual native Plan Mode UI were not exercised. Read-only sandbox behavior and default no-report-write
behavior were exercised. No external posting, code remediation, or Git mutations occurred.

## Packaging

Skill validator, metadata parsing, reference link checks, scoped Prettier checks, and `git diff --check`
pass. Source entrypoint is 791 whitespace-delimited words. Reference directories are included through
scoped Git and Prettier exceptions; raw evaluation results are excluded from formatting. README documents
global installation, BRD/executor handoff, output, and read-only defaults.

Existing repository-wide formatting failures were recorded during the preceding execute-plan work;
this change uses scoped checks and does not modify those unrelated files. No commits were requested
or created in the source repository.
